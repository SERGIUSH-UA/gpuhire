# Журнал змін

Формат — [Keep a Changelog](https://keepachangelog.com/uk/1.1.0/), версії —
[SemVer](https://semver.org/lang/uk/). До 1.0 мінорна версія може міняти
поведінку; те, що описано в [`docs/contract.md`](docs/contract.md), у межах
мінорної версії не міняється.

## [Unreleased]

## [0.3.1] — 2026-09-24

Перший публічний випуск гілки 0.3: тег `v0.3.0` є, але на PyPI він не вийшов —
сторож sdist приймав фікстури `tests/data/` за приватну `data/`. Сторож тепер
дивиться лише корінь пакета.

### Змінено

- 🔴 **Вибір машини — від цілі темпу.** Новий параметр плану `target_pph`
  (типово 5 000 стор/год на еталонній сторінці; на густішому матеріалі ціль
  знижується пропорційно накладним сторінки) і `max_wait_min` (типово 60).
  Машина береться, лише якщо прогноз × 0.86 ≥ цілі і тисяча сторінок ≤ $0.10;
  серед придатних — найдешевша очікувана тисяча з поправкою на надійність.
  Порожній ринок — чекання, потім `market_empty`; слабшої машини наглядач не
  бере. CLI: `--target-pph`, `--max-wait-min`.
- **Прибрано** тіри, `settled_for_less`, `prefer_min_cores`,
  `wait_for_cores_min`, `time_value_usd_per_hour`: ключі в плані лише
  попереджають. `min_pages_per_hour` діє тільки з `target_pph = 0`.
- Пошук Vast фільтрує ядра й ціну за ціллю на стороні клієнта, стеля $0.365
  рахується на карту, ліміт видачі — 100.
- Шарди пакуються по кожній карті окремо; стеля регулятора виводиться з
  виміряних ядер і пам'яті, а не дорівнює стартовому числу.
- **Раннер читає чергу одним флотом**: шарди вантажать моделі один раз і
  йдуть справами (≤ 400 сторінок) поспіль; підготовка наступних справ іде
  паралельно; справа закривається, щойно всі шарди з неї вийшли, у будь-якому
  порядку; запас підготовки міряється непрочитаними сторінками. Потребує
  Нишпорки з `--queue`; зі старими ассетами раннер іде справа за справою.

### Додано

- Вирок `underdelivers`: бокс, що три тіки поспіль дає < 0.85 × обіцяного темпу
  (після 15 хв і 200 сторінок), гаситься й замінюється; вирок `below_target`
  у реєстрі машин.
- Добір із живої машини: справи, яких сховище не дало повністю, забираються з
  боксу (лише вони) перед його знищенням; інцидент `store_short`.
- **Облік за рахунком Vast.** Наглядач тягне списання по своїх інстансах
  (`/charges`: GPU, диск, трафік) кожні 15 хв і на фініші; витрачене —
  більше з «ціна × час» і рахунку, `bill_factor` поправляє прогноз.
  `gpurunner htr bill --days N` звіряє сесії й журнал витрат заднім числом.
- **Ціна машини включає трафік хоста**: ГБ кадрів черги × `inet_down_cost`
  додаються до ціни й до стелі тисячі сторінок у виборі та воротах; у рядку
  оренди — `у т.ч. трафік $X`.
- **Захід переживає смерть наглядача.** Бокс кожні 5 хв кладе в сховище
  серцебиття (`heartbeat/<plan_id>.tgz`: стан, прогрес, лог раннера);
  `gpurunner htr recover --plan --session` забирає прочитане, звіряє повноту,
  рахує гроші за рахунком і гасить бокс.
- `gpurunner boxes absolve M --since … --why …` — зняти з машини вироки, які
  спричинив наш код, зберігши журнал.
- Перед забором наглядач просить раннер залити чекпоінт негайно; добір із
  боксу — дельтою одним архівом, SFTP лише запасний шлях.

### Виправлено

- 🔴🔴 **Чекпоінти більше не вичерпуються.** Останній presigned-слот
  перезаписується знімком усього після передостаннього; доти після вичерпання
  слотів чекпоінтів не було до кінця заходу, і прочитане гинуло з машиною.
  Кілька невдалих заливок поспіль видно наглядачу як `exhausted`.
- `done` ставиться лише з текстом моделі плану; текст іншої моделі в тій
  самій теці відкладається в `_prev_<модель>/`.
- Теки виводу й ключі сховища несуть шифру архіву, якщо номер справи вже
  зайнятий іншим архівом (`spr-66__DAHmO-315-66`, `cases/<шифра>/…`).
- `offline`/`error` убивають бокс лише за три тіки поспіль; аварійний забір
  не пише в реєстр `ok`; догін бере лише недочитані справи; `boot_sec` — лише
  підйом хоста, заливка окремо в `upload_sec`.
- **Качання кадрів докачує, а не починає з нуля.** Частини діапазонного
  качання живуть між спробами (реєстр `<ім'я>.dl.json` з розміром і ETag);
  невдале передзавантаження віддає привезене звичайному качанню. Доти одна
  впала частина стирала всі вісім, і том 5.6 ГБ качався з нуля шість разів.
- Сторінки боксу рахуються монотонними лічильниками раннера, а не з індексів
  справ: «перестав читати» більше не спрацьовує на здоровому боксі.
- Нова оренда отримує власний годинник підйому; нічого не лишилось читати —
  оренди немає.
- `underdelivers` судить бокс за більшим із середнього й усталеного темпу;
  прогноз бюджету не бере застарілого вікна.
- Ворота знижують темп за паралельним каналом; ціль, зрізана лише каналом, —
  вирок мережі `slow_for_data`.
- Проба заліза переживає одне обірване SSH-з'єднання на свіжому боксі
  (3 спроби; відмову в автентифікації не повторює).
- Калібрування: справа з < 5 сторінок на шард міряє старт, а не темп.
- Кадри заливаються рівно під ключем плану (з тегом архіву).

- 🔴🔴🔴 **27 хвилин підйому боксу: причину знайдено — `job.py` важив 48 МБ.**
  У джерело роботи вшиваються presigned-посилання ВСІЄЇ черги, а слоти чекпоінтів
  рахувались від стелі часу ЗАХОДУ (14 год ⇒ 548) і так — на КОЖНУ справу. На
  черзі з 194 справ це **121 832 посилання** по ~400 символів = 48 МБ джерела.
  Файл їхав на машину paramiko-SFTP **синхронними шматками по 32 КБ**: ціна тут
  не смуга, а `розмір / 32 КБ × круговий оберт`, тобто півтори тисячі обертів до
  Кореї. Заміряно 23.09.2026: підйом 1644 с, із них ~25 хвилин на цей файл, і в
  лозі це виглядало як ТИША (SSH відповів за 131 с, далі жодного рядка).

  Дві половини, обидві полагоджені:

  1. **Слоти — від розміру справи**, не від стелі заходу (`CKPT_PAGES_PER_SLOT`,
     одна точка на ~100 сторінок, підлога 8, часова стеля лишається межею
     зверху). Та сама черга: 121 832 → 3 104 посилання, `job.py` 48 МБ → 1.2 МБ.
  2. **`set_pipelined(True)`** на записі тексту: запис стає потоковим, і
     наступний великий файл не платить хвилинами.

  🪤 Сторож конвеєра спершу вижив під мутацією: він шукав `set_pipelined(True)`, а
  це саме слово стоїть у ДОКСТРІНГУ тієї ж функції. Тепер прив'язка до виклику
  (`fh.`). Третій такий випадок за день — сторож, що читає джерело, ловить
  власний коментар.

### Виправлено

- 🔴🔴 **paramiko SFTP прибрано із заливки входів job'а** (`_upload_inputs`).
  ⚠ Це той самий канал, про який я двічі звітував «прибрано з передачі даних» —
  маючи на увазі забір і службові файли. Заливка входів лишалась на ньому, і
  звіт був неправдивий.

  Заміри одного плеча (орендований бокс, 150 МБ): paramiko SFTP **0.48 МБ/с**,
  системний `scp` 7.56, HTTP 20.07. Плюс на кожен файл ішло ДВА обміни (`put` і
  `stat`) — тека з тисячі кадрів платила дві тисячі кругових обертів через
  океан. Тепер тека пакується в ОДИН tar, їде системним `scp` і розпаковується
  на машині; приймач подвійний — розмір архіву на машині і КІЛЬКІСТЬ
  розпакованих файлів.

  Чому тут `scp`, а не HTTP, яким їде решта: склад на цей момент ще не піднятий
  (його ставлять ворота, і пізніше), а сам цей шлях працює лише там, де ні
  бакета, ні складу немає — при них теки входів порожні за побудовою. Тобто
  вибір був між 0.48 і 7.56.

  Сторож дивиться в ДЖЕРЕЛО функції: поведінковий тест цього не ловить, бо фейк
  підміняє обидва канали й обидва «працюють».

### Аудит paramiko, повний

Щоб більше не звітувати зайвого. Дані: `_download_tree` і три службові файли у
`fetch_outputs` — це ШЛЯХ АВАРІЇ (сховище не дало нічого; за 99 заходів туди
доходило 8), лишені свідомо. Решта викликів — кілобайти керування: `job.py`,
скрипт складу, читання `_status.json`/`_progress.json`/`_runner.log`, стоп-кран
`NOSTOP`. Це не канал і на час заходу не впливає.

### Виправлено

- 🔴🔴 **Вибір машини нарешті бачить ЧАС ПІДЙОМУ.** Прогноз давав усім однакові
  5 хвилин холодного старту, а хост тягне образ СВОЇМ каналом: 23.09.2026 бокс у
  Кореї піднімався **27 хвилин** — у п'ять разів понад сталу, і це оплачений
  `docker pull`, а не робота. Реєстр цей час пам'ятав (`measured.boot_sec`) і
  навіть пояснював його як «єдиний сигнал про закешованість образу», але в
  РОЗРАХУНОК він не йшов: машина, про яку ми знаємо, що вона підніматиметься пів
  години, важила стільки ж, скільки та, що піднімається за 40 с.

  Тепер `predict_hours`/`predict_cost` беруть `boot_sec` із реєстру — те саме
  правило, яке вже діяло для темпу («замір б'є модель»). Незнання не стає нулем:
  без заміру лишається стала.

  🪤 Сторож цієї правки спершу лишився зеленим під мутацією — шукав ПІДРЯДОК
  `boot_sec=boot_sec`, а той стояв у другому виклику. Тепер перевіряє кількість:
  підйом мусить дійти і в години, і в ціну.

### Виправлено

- 🔴🔴 **Заливка на бокс іде ВІСЬМОМА потоками, а не одним.** Це та сама вада, від
  якої сьогодні ж поставлено двопотокову пробу каналу, тільки в зворотний бік:
  там ми МІРЯЛИ одним потоком, а качали вісьмома; тут міряли вісьмома, а ВЕЗЛИ
  одним. Заміри 23.09.2026: домашній канал на 40 МБ — один потік 3.83 МБ/с,
  вісім 8.94 — АЛЕ чергований замір показав, що канал гуляє 1.05-8.94 МБ/с між
  прогонами (медіана ~2.2, відношення медіан 0.97), тобто те порівняння нічого не
  доводило. Що справді заміряно: жива доставка 135 МБ одним потоком у Корею —
  1.2 МБ/с. Змінено з іншої причини — тієї самої, з якої транспорт на боксі вже
  ескалює до восьми діапазонів: на великому колі один потік обмежений добутком
  смуги й затримки. Доказом буде A/B на ОДНІЙ машині.

  Файл ріжеться на вісім шматків, вони їдуть паралельно й склеюються НА МАШИНІ
  однією командою (`cat … && rm …`, імена крізь `shlex.quote`). Дрібне (до 8 МБ)
  лишається одним потоком — вісім рукостискань на ньому дорожчі за виграш.
  🔴 Після склейки розмір звіряється тим самим каналом: склейка без шматка дала б
  файл, який ВИГЛЯДАЄ цілим, і виявилось би це вже на оплаченій машині.

  ⚠ Заміряного виграшу на живому боксі ще немає: другий пробний захід загинув до
  доставки (наглядач помер разом із середовищем, поки сусідня сесія працювала в
  цьому ж репозиторії). Чотири мутації червонять свій тест, але число з живої
  оренди тут лишається незнятим.

### Виправлено

- 🔴🔴 **Рятунок більше не викидає кеш сегментації.** У `fetch_ckpt.unpack` стояло
  `continue` з коментарем «data/, logs/ — не декод», тобто `htr fetch-ckpt`
  викидав `data/derived/htr_seg/`. Наслідок видно не одразу й не в лозі:
  перечитування тієї ж справи другою моделлю мусить брати готову сегментацію
  (замір 18.4 → 9.1 с/стор, тобто ВДВІЧІ дешевше), шукає її на диску й не
  знаходить — бо забір її не приніс. У плані це тихо ставало «засіву немає», і
  кожен повторний прогін платив за сегментацію наново. Штатний забір наглядача
  кеш приносить (`_remap` лишає `data/…` як є), тож рятунок тепер дає те саме;
  логи шардів теж не викидаються.

  Знайдено фактом: жодна з 237 справ завершеного заходу не мала кешу на диску,
  хоч у чекпоінтах R2 він є. Перевірено на справжньому чекпоінті з R2.

### Додано

- **Проба міряє канал ТАК, ЯК ним користується транспорт: одним потоком і
  вісьмома.** Транспорт на боксі ескалює до восьми діапазонних з'єднань нижче
  4 МБ/с, тобто однопотокова проба міряла не те, чим потім качають, і занижувала
  саме там, де транспорт виграє — звідси й бралось «раз 10 МБ/с, раз 1.5»: часто
  це той самий канал, зміряний одним потоком і використаний вісьмома. Обсяг у
  обох замірах ОДНАКОВИЙ (32 МБ), бо порівнюються два способи, а не два розміри.
  Ворота качання архіву тепер рахують час тим числом, яким архів справді поїде;
  «мертвий канал» і плоска межа лишаються на однопотоковому.

- 🔴 **Рятунок у режимі `box`.** Доти його не було взагалі: у бакеті на цей
  випадок лежать присигновані посилання, записані в ПЛАН, а в режимі `box` план
  про машину знати нема звідки — її на час складання ще не існувало. Помер
  наглядач після читання й до забору — і прорахований текст діставати нічим,
  хоч він лежить на живій машині. Тепер адресу складу разом із секретом заходу
  пише в стан той, хто її дізнався (`SupervisorState.origin_base`), а
  `htr fetch-ckpt` бере її звідти сам і будує посилання з `ckpt_prefix` та
  `ckpt_slots`, які план якраз несе. Імена чекпоінтів складає одне місце
  (`box_transport.ckpt_names`): два списки, зроблені окремо, розійшлись би тихо
  й помітно це стало б у той день, коли точка відновлення знадобилась.

### Виправлено

- 🔴 **Віддача чекпоінта дістала стелю часу — її не було взагалі.** `curl` без
  `--max-time` на завислому сокеті стоїть навічно, а стоїть він посеред циклу
  ЧИТАННЯ: оплачувана машина перестає рахувати сторінки, прогрес не
  оновлюється, і зовні це виглядає як «працює». Стеля рахується від розміру з
  підлогою (аварійна, не робоча), і сторож тепер дивиться на КОЖНУ віддачу
  раннера, а не на конкретний виклик: наступна поїде тим самим `curl`.

- 🔴 **Стеля переносу більше не стала.** `timeout=1800` був хибний в обидві
  сторони: на швидкому каналі — пів години на мертвому сокеті після того, як
  усе вже зрозуміло, на повільному — розріз живого переносу посередині. Канал
  гуляє на два з половиною порядки (282 машини реєстру: медіана факт/обіцянка
  0.266, мінімум 0.004), тож одна стеля для 0.004 і 20 МБ/с не може бути
  правильною для обох. Тепер це обсяг, поділений на ЗАМІРЯНИЙ канал, із запасом
  і рамками; заміру ще немає (перший перенос заходу) — береться песимістична
  підлога.

- 🔴 **Канал до машини їде в реєстр** (`uplink_mbs`, `uplink_via`). Доти замір
  жив нотаткою сесії й помирав разом із нею: наступний захід про аплінк до тієї
  самої машини не знав нічого. Це окреме число від `net_mbps` проби — та міряє
  шлях ХОСТА до бакета, а це наш канал до цієї машини, і саме їхня різниця
  робила повільну доставку схожою на швидку машину.

- **Склад не чекає вічно на клієнта, який перестав слати.** Оголосив тіло
  більше, ніж надіслав (обрив, вбитий процес, повний диск на його боці) — і
  склад тримав потік разом із недописаним `.part`, доки не здасться TCP. Стеля
  стоїть на одне читання, тож цілого чекпоінта не ріже.

### Змінено

- Набір тестів: 196 → 44 с на 1280 тестів. Найповільнішим був новий тест
  обірваної заливки (10.2 с) — і виявилось, що платив він саме за ваду складу
  вище, а не за роботу.

### Змінено

- 🔴🔴 **Плече дім→бокс переїхало зі `scp` на HTTP.** Заміряно на орендованому
  боксі 20.09.2026, 150 МБ: SFTP paramiko 0.48 МБ/с, зворотний тунель 0.49,
  системний `scp` 7.56, HTTP одним потоком **20.07**. Те саме залізо й той
  самий канал — розкид у 42 рази вирішує протокол, не смуга. На архіві 2.25 ГБ
  різниця між `scp` і HTTP — п'ять хвилин проти чверті години, і та чверть
  години йде ДО першої прочитаної сторінки, на вже оплаченій машині.

  Порядок доставки обернуто: склад (`htr/origin.py`) піднімається ПЕРШИМ, і дані
  їдуть у нього `PUT` — тим самим шляхом, яким раннер потім робить `GET`.
  Чекпоінти назад на машину (переоренда) теж ідуть `PUT`, без окремого
  рукостискання SSH на кожен.

  🔴 Плече вибирається ЗА ВИМІРОМ, а не за здогадом. Це два різні питання, і
  жодне не вгадується: чи Vast прокинув порт назовні (зовнішній порт
  випадковий, читається з інстансу) і чи він досяжний ЗВІДСИ (фаєрвол хоста,
  NAT, інший дата-центр). Склад відповів на `/healthz` — веземо HTTP; ні —
  веземо `scp` і **кажемо це вголос** нотаткою з причиною. Мовчазний перехід на
  вчетверо повільніше плече означав би дорожчий захід без видимої причини.

  Порт просимо ПРИ ОРЕНДІ (`env: {"-p 8752:8752": "1"}`) і на обох шляхах —
  і job, і гола оренда: доданий пізніше порт вимагає перестворення інстансу,
  тобто іншої машини за інші гроші. Проситься завжди, навіть коли транспорт
  заходу — бакет: зайвий прокинутий порт не коштує нічого.

  Секрет складу тепер один на весь захід, народжується ДО сабміту (посилання з
  ним лягають у параметри роботи ще до оренди) і їде на машину **файлом-
  запускачем із правами 600**, а не в рядку команди: `ps` видно кожному
  сусідові по хосту, і префікс `VAR=... python3` теж видно — його бачить
  оболонка, і саме її рядок лишається в `ps`.

  Сім мутацій перевірено, кожна червонить свій тест.

### Виправлено

- Склад має `/healthz` — окрему відповідь «живий», **під секретом**. Доти проба
  питала про `_origin.py`, тобто про файл, який існує лише тому, що ми його
  щойно поклали; проба на побічній обставині ламається щоразу, коли обставина
  змінюється. Без секрету `/healthz` був би маячком «тут склад» для будь-якого
  сканера портів — рівно тим, від чого стоїть 404 замість 403.

- 🔴 **Відмова складу тепер доїжджає й на Linux, і CI зелений.** Тест від
  попереднього коміту тримав його червоним чотири коміти: `urlopen` дописує
  тіло ДО КІНЦЯ і лише потім читає відповідь, тож на 16 МБ він діставав
  `BrokenPipeError` там, де склад чесно відповів 404. На Windows усе ховалось у
  буфері сокета — тобто тест міряв розмір буфера ОС, а не поведінку складу.
  Сам склад теж був не до кінця правий: недочитаний хвіст лишався в сокеті
  живого з'єднання `HTTP/1.1`, і сервер розбирав би сотні мегабайтів чужого
  чекпоінта як наступні запити. Тепер `_drain` каже, чи дочитав, і відмова з
  недочитаним тілом несе `Connection: close`; дрібна відмова з'єднання не рве.
  `handle_error` більше не друкує трасування на кожен обрив: на боксі це той
  самий лог, у якому потім шукають причину падіння справи.

### Виправлено

- 🔴🔴 **SFTP зник зі службових файлів.** Тіло результату приїжджає зі сховища
  одним об'єктом на чекпоінт, а після нього лишався «дотяг лише службового»
  (`only_meta=True`) — і на черзі він вироджувався в `listdir` на КОЖНУ справу
  плюс `get` на КОЖЕН лог шарда: ~2600 обертів через океан на 236 справах. Фаза
  забору висіла понад 30 хвилин, з'їла свою стелю й лишила всі 237 справ без
  вердикту — при цілому тексті (5115/5115) удома.

  Тепер раннер складає службові файли справи (лог раннера, стан, прогрес, логи
  шардів) в один `service.tgz` і віддає його тим самим presigned `PUT`, яким
  віддає чекпоінти; дім бере його одним `GET`. Віддача стоїть у `finally`
  кожної справи, а не в кінці заходу: діагностика справи, після якої бокс
  помер, і є та, яку треба розбирати. При складі на самій машині архіви їдуть
  додому одним `tar` і одним з'єднанням — різниця з колишнім SFTP не в
  протоколі, а в кількості обертів: два виклики на всю чергу замість двох на
  кожну справу.

  Пофайловий обхід теки по SFTP лишається ОДНИМ шляхом — коли у сховищі не
  виявилось нічого (8 заходів із 99). Заміри одного плеча на орендованому
  боксі: SFTP paramiko 0.48 МБ/с проти 20.07 у HTTP, у 42 рази; за 99 заходів
  SFTP не дав повноти самостійно НІ РАЗУ і п'ять разів зіпсував вердикт уже
  привезеному текстові.

  Сім мутацій перевірено. Прибрано `only_meta`, `_fetch_shard_logs`,
  `SHARD_LOG_MAX_SEC` і мертвий слот плану `result_put_url`, який не писав і не
  читав ніхто — готовий слот, що виглядав як зроблена робота.

### Змінено

- Сторожі, що читають ДЖЕРЕЛО, більше не ріжуть його фіксованим вікном.
  `src[i:i + 2500]` і `src[i:i + 4000]` за один вечір двічі зламались від
  ДОДАНОГО КОМЕНТАРЯ й повідомили про відсутність того, що на місці, — а хибне
  червоне послаблюють, аж поки сторож не перестане ловити свою ваду. Межа тепер
  структурна (`tests/srcprobe.py`).

### Виправлено

- 🔴🔴 **Кадри більше не заливаються вдруге.** Ключ архіву справи
  сталий (`cases/<слуг>.tar`) — він не несе ні дати, ні номера заходу, тож
  кадри першої заливки лежать у бакеті й далі. Складач однак пакував і заливав
  їх наново перед КОЖНИМ прогоном: заміряно 23.09.2026 — 237 справ, 24 ГБ у
  бакеті й 10–15 хвилин домашнього аплінка перед заходом, який сам триває
  хвилини. Тепер про кожен архів питається сам бакет
  (`plan_build.already_in_bucket` через новий `r2.head`), і заливається лише те,
  чого там немає або що змінилось. Те саме для архіву ассетів — другого за
  розміром шматка аплінка.

  Прапорець `--skip-upload` для цього існував із самого початку, і саме тому
  вада прожила довго: **можливість була, а дією її мусила зробити людина**,
  причому людина, яка не може знати напевно, чи об'єкт на місці. Помилка в бік
  «пропустити» дає посилання в порожнечу (404 на пробі каналу, змарнована
  оренда — 06.09.2026), тож прапорець лишається, але нічого не вирішує:
  рішення ухвалює відповідь бакета про конкретний об'єкт.

  Збіг перевіряється двома умовами, і потрібні обидві: **той самий розмір**
  (інакше кадри додали, прибрали чи перезняли) і **об'єкт новіший за
  найсвіжіший кадр** (інакше кадр підмінили після заливки, а розмір випадково
  зійшовся). Розмір майбутнього архіву рахується БЕЗ пакування — заголовки
  складає той самий `tarfile`, тож довге ім'я й кирилиця з розширеним
  заголовком PAX ураховуються точно; приймач звіряє передбачене зі спакованим
  до байта. Будь-яка невизначеність (немає об'єкта, бакет не відповів, не
  збіглось) означає заливку, і кожна відмова називає причину в лозі.

## [0.2.5] — 2026-09-22

Вечір одного заходу (`spr-160`, архів 2.25 ГБ, бокс RTX A4000×4 в Японії), який
тричі впав на качанні кадрів і не прочитав жодної сторінки.

### Виправлено

- 🔴🔴 **Обрив качання більше не скидає привезене.** `curl` рве з'єднання після
  60 с нижче підлоги швидкості (`rc=28`), і повтор починався з нуля. На архіві
  2.25 ГБ при виміряних 21.6 Мбіт/с це 14 хвилин, які три спроби з'їдали тричі
  поспіль — а справа однаково падала. Тепер повтор **продовжує з байта, що вже
  на диску**, діапазонними з'єднаннями; якщо продовження не пішло, лишається
  перевірений чистий старт. Умова безпеки — файл мусить бути префіксом об'єкта,
  і обидва шляхи запису (один потік і склейка діапазонів) її тримають.

- 🔴🔴 **Ворота відсіюють бокс за ціною входу, а не лише за роботою.** Офер
  обіцяв 1821 Мбіт/с, проба дала 21.6 — у 84 рази менше. Машина пройшла **всі**
  ворота, бо ядра, ціна й темп у неї були добрі: про обсяг першого архіву не
  питав ніхто, а він мусить лягти цілком, перш ніж прочитається бодай одна
  сторінка. `Need.max_archive_mb` + перевірка в `gate.evaluate`: відмова, якщо
  найбільший архів черги тягнеться довше за `min(12 хв, max(10 хв, 25% прогнозу))`.
  Стоїть після проби й до заливки, тож коштує ~30 с замість 24 хвилин оренди.

### Додано

- **Чорна скринька наглядача** (`supervise/blackbox.py`). Два наглядачі поспіль
  зникли посеред заходу, лишивши бокс горіти, і на диску не було нічого: ні
  трасування, ні вердикту, ні події в журналі Windows. Тепер інструментовані всі
  шляхи смерті, які процес може зафіксувати сам — необроблений виняток, виняток
  у потоці, `faulthandler`, сигнали, `atexit`, зникнення батька, — плюс хлібні
  крихти раз на 15 с. 🔴 Головне: **тиша теж стала доказом** — крихти до
  останньої секунди без запису про причину означають `TerminateProcess` ззовні,
  а не «просто зник». Розтин друкується разом із вердиктом `orphaned`.

- **Діапазонне качання як рятунок повільного маршруту.** Архів їде одним
  потоком, і лише коли виміряний темп не дотягнув до 4 МБ/с за 45 с, решта
  береться вісьмома з'єднаннями з дописуванням до привезеного. Безумовну
  паралель відкинуто заміром: на здоровому каналі один потік дав 20.07 МБ/с
  проти 15.95 на восьми.

## [0.2.4] — 2026-09-22

### Виправлено

- 🔴 **Усі адреси пакета вели в приватний репозиторій.** `Homepage`, `Source`,
  `Issues` і `Changelog` показували на `SERGIUSH-UA/gpurunner`, який закритий:
  для кожного, хто не власник, це 404 — перевірено запитом без авторизації.
  Тобто пакет на PyPI публічний, а його код, журнал змін і місце для скарги —
  ні. Туди ж вели `LICENSE`, `CONTRIBUTING.md`, `SECURITY.md` і сторінки
  релізів, тобто саме те, що додавалось заради публікації.
  Тепер вони ведуть у **`SERGIUSH-UA/gpuhire`** — публічне дзеркало з тим
  самим чистим деревом і тими самими тегами. Приватний репозиторій лишається
  домом розробки: його історія заражена від кореневого коміта (пошта автора,
  шляхи робочих просторів, імена приватних проєктів), і перемикач видимості
  відкрив би її всю, а не чисте дерево.
  ⚠ Виправлення не могло доїхати раніше: PyPI не дає правити метадані
  випущеної версії, а сторінка проєкту показує адреси ОСТАННЬОГО релізу. Тому
  0.2.0–0.2.3 лишаються з неробочими посиланнями назавжди.
- `git clone … && cd gpurunner` у README і CONTRIBUTING став `cd gpuhire`:
  клон створює теку за іменем репозиторію, і стара команда обривалась на
  другому кроці.
- `SECURITY.md` показує на вкладку Security того репозиторію, куди стороння
  людина справді має доступ; приватні звіти про вразливості там увімкнено.

### Змінено

- README називає різницю імен повністю: `pip` і `git clone` кажуть `gpuhire`,
  а команда, модуль і змінні оточення лишаються `gpurunner`. Доти блок
  пояснював лише ім'я на PyPI, і репозиторій під новим іменем виглядав би
  помилкою.

## [0.2.3] — 2026-09-22

### Виправлено

- **Причину падіння справи з'їдало підписане посилання.** Помилка
  завантаження виглядає як `curl <URL> провалився (rc=NN)`, а підписане
  посилання R2 саме по собі 368 символів: раннер ріже запис до 400, тобто
  причина трималась на восьми символах запасу, а наглядач ріже до 300 — там
  її не лишалось уже точно. 21.09.2026 три справи черги впали поспіль, і в
  усіх трьох записах лишився самий підпис, обрізаний на `X-Amz-SignedHeader`:
  ні коду curl, ні тексту не збереглося ні в стані, ні в журналі, ні в
  інциденті. Діагноз за таким рядком ставиться навмання — і був поставлений
  неправильно («посилання протухло»), хоча в тому ж рядку стояло
  `X-Amz-Expires=86400`, тобто доба.

  Тепер раннер кладе в помилку **ім'я файла**, а наглядач згортає будь-який
  URL до впізнаваного й обрізає текст **із середини**, бо причина буває і на
  початку, і в хвості:

  ```
  curl …/spr-576.tar?… -> rc=23: No space left on device
  ```

## [0.2.2] — 2026-09-21

### Виправлено

- **Підлога темпу діяла мовчки, і «наглядач нічого не обирає» було не
  відрізнити від «мій поріг відсік ринок».** Тепер при виборі машини захід
  каже, скільки машин відсікла підлога, скільки лишилось і чи є запас, а коли
  ринок після неї порожній — вирок називає ЧИСЛО, до якого її знижувати:
  найшвидшу машину, яку ринок дає на цьому матеріалі. Заміряно на живому
  ринку: 1000 стор/год ріже 9 машин із 81 на метриках і 24 з 25 на протоколах
  консисторії — перше запас, друге лезо, і людина має бачити, на якому вона
  боці, до того як ринок просяде.
- **Порада при порожньому ринку була про іншу ручку.** Причина відсіву
  підлогою звучить «… стор/год < підлоги 1000», тобто містить «год», і
  класифікатор причин відносив її до стелі ГОДИН — вирок радив підняти
  `--max-hours`, що на підлогу не впливає ніяк.
- **Два тести бекенда Modal були червоні на Python 3.11 і 3.13.** Вони
  подають job із дефолтним образом, а дефолт — 3.12; бекенд при розбіжності
  локальної й віддаленої версії законно відмовляє ще до збирання образу. Самі
  тести про таймаут, а не про версію, тож тепер просять образ під той
  інтерпретатор, на якому біжать.

### Змінено

- `Selection` несе відкинуті оффери й тоді, коли вибір **відбувся** — доти
  вони жили лише у вироку «ринок порожній», тож питання «скільки машин з'їла
  моя підлога і чи лишився запас» не мало відповіді доти, доки не ставало
  пізно.

## [0.2.1] — 2026-09-21

Захід брав найповільнішу машину ринку і гасив здорові бокси: три вади з
одним наслідком — прогін ішов утричі повільніше за те, що залізо дає за ті
самі гроші, — і поріг, якого для цього бракувало.

### Виправлено

- **Виміряний темп із реєстру переносився між різними офферами однієї
  машини.** Вердикт лежить на `machine_id`, а машина продає кілька офферів:
  у 140182 їх два — 12-ядерний і 6-ядерний на тій самій карті. Темп
  918 стор/год, виміряний на 12 ядрах і на матеріалі з 66 рядками на
  сторінку, приписався 6-ядерному офферу під матеріал зі 138 рядками, і той
  виграв ранжування в машини, яка була вдвічі швидшою. Тепер замір
  масштабується відношенням модельних темпів «за умов заміру» і «за умов
  цього оффера»: там, де умови збігаються, відношення дорівнює одиниці й
  замір застосовується як застосовувався. Той самий замір на тому самому
  офферi дає 218 стор/год замість 918.
- **Прогноз витрат рахувався темпом разом із розгоном.** `pages_per_hour`
  раннера — це сторінки, поділені на весь час від старту шардів, тобто зі
  стартом процесів і завантаженням ваг у знаменнику. Регулятор поруч міряє
  сталий хід (`rates {шардів: стор/год}`), і обидва числа лежать в одному
  прогресі. Бюджетна гілка брала гірше з двох і гасила здорові бокси: «за
  виміряним темпом (371 стор/год) вийде $0.57 при бюджеті $0.50» — при
  `rates {"7": 758}` у тому ж файлі стану. Тепер сторінки діляться на сталий
  темп, а розгін додається стільки разів, скільки справ іще попереду, — тобто
  не зникає з прогнозу, а переїжджає туди, де справді буде.
- **Відчеплений запуск падав на справі, чия назва має нелатинські літери.**
  Пускач WSH писався в ASCII, тож шлях зі скрипта клав `UnicodeEncodeError`
  ще до планувальника: захід не стартував, на диску лишався порожній `.vbs`,
  а назовні це виглядало як «наглядач не доповів про старт». WSH читає VBS у
  UTF-16 за BOM.

### Додано

- **Підлога темпу `min_pages_per_hour`** — поріг вибору машини, окремий від
  стелі ціни, бо вони про різне. Стеля за тисячу сторінок пропускає скільки
  завгодно повільну машину, аби дешеву: 6-ядерний бокс за $0.051/год дає
  $0.234 за тисячу — усередині будь-якого розумного порога — при 218 стор/год,
  тобто 3.7 години на чергу, яку багатоядерна машина читає за півгодини.
  Підлога **не послаблюється тірами**, на відміну від ціни й VRAM: порожній
  ринок має закінчуватись чесним `market_empty`, а не згодою взяти будь-що.
  Ручка плану або `-p min_pages_per_hour=…`; чинне значення видно в рядку
  чинних ручок.

## [0.2.0] — 2026-09-21

Перший публічний випуск. Версія 0.1 жила приватним інструментом одного
дослідження; нижче — те, що змінилося на шляху до публікації.

### Додано

- **Доставка без бакета: `htr plan --transport box|auto`.** Хмарний прогін
  доти вимагав свого S3 навіть від того, хто просто орендував машину на
  годину: раннер уміє брати дані лише `curl` по посиланню, а видати таке
  посилання вмів тільки бакет. Тепер його може видати сама машина — кадри й
  ассети кладе туди системний `scp` (7.56 проти 0.48 МБ/с у SFTP paramiko,
  заміряно на орендованому боксі), а на її петлі піднімається крихітний склад
  (`htr/origin.py`, GET/PUT, лише `127.0.0.1`). Раннер не змінюється жодним
  рядком. Доставка йде ПІСЛЯ воріт — інакше кожна забракована машина коштувала
  б заливки; канал хоста до інтернету при цьому не міряється взагалі.
  🔴 Чекпоінти на такій машині гинуть разом із нею, тож наглядач везе їх
  додому кожні п'ять хвилин ПІД ЧАС роботи, а не в кінці. `auto` (дефолт)
  бере бакет, коли він налаштований. Передполіт для `box` перевіряє диск, а не
  мережу: посилань ще не існує, їх видасть машина, якої поки немає.
- **Машинний вивід `--json` у `run`, `status`, `fetch`.** Останній рядок stdout,
  що починається з `{`, — один JSON-об'єкт зі `schema`; людське з `--json` іде в
  stderr, код виходу той самий, відмова теж відповідає об'єктом. Стан прогону —
  із закритого переліку `queued · running · completed · failed · cancelled ·
  unknown`. Споживачі більше не мусять розбирати текст.
- **`docs/contract.md`** — на що мають право спиратись програми, які кличуть
  gpurunner: команди й прапорці, форма відповідей, схема `plan.json`, змінні
  оточення.
- **Плагін оренди для Нишпорки** — entry point `nyshporka.cloud`
  (`gpurunner.plugins.nyshporka_cloud:VastRent`). gpurunner орендує й гасить бокс
  на Vast.ai (скоринг офферів, реєстр боксів, перевірка балансу, незнімна стеля
  ціни), а заливку, прогін і забір Нишпорка веде сама по SSH; R2 не потрібен.
  Понад контракт — `status()`, `login(api_key)`, `estimate(need)`. Опис —
  [`docs/nyshporka.md`](docs/nyshporka.md), форма — `docs/contract.md` §6.
- Vast: **оренда без job'а** — `VastBackend.rent_box / destroy_box / box_info /
  live_instances`. onstart такого боксу — лише жорсткий таймер самознищення;
  будь-який збій після створення інстансу (зокрема Ctrl+C) гасить його.
- **SSH-ключ без рук:** `auth.vast.ensure_ssh_key()` генерує пару ed25519 у
  `<config>/vast_ssh/`, коли ключа немає ні в `GPURUNNER_VAST_SSH_KEY`, ні в
  `~/.ssh`. Наявний ключ не перезаписується ніколи; свій має пріоритет.
- **`gpurunner auth vast --key <КЛЮЧ | ->`** зберігає ключ API у
  `<config>/vast_api_key` (`-` — зі stdin); ключ ніде не друкується.
- Реєстр боксів: вирок **`slow_run`** (тиждень, бан із двох ударів) — машина
  читала повільніше за прогноз за словами викликача, що орендував без наглядача.
- Job **`rukopys_ocr`** — сторінкове читання моделлю Rukopys-OCR-4B.
- Vast: параметр **`min_cuda`** (мінімальна CUDA драйвера хоста).
- **`tools/scan_private.py`** — ворота проти приватних даних, у pre-commit і CI.
- `LICENSE` (MIT), `CONTRIBUTING.md`, `SECURITY.md`, extra `all`.
- CI (Linux 3.11–3.13, Windows 3.12) і реліз за тегом через Trusted Publishing.
- Наглядач попереджає, коли стартує з порожнім реєстром боксів.
- `gpurunner htr calibrate` показує медіану окремо по старому й новому раннеру
  та стелю за ядрами як вимір сітки. Саме розрив між раннерами був ранньою
  ознакою того, що модель поїхала, — одне спільне число його ховало.
- Стан заходу зберігає `pages_this_run` і `wall_sec` кожної справи. Доти
  `pages_done` включав підняте з чекпоінтів, а темп рахувався лише від
  зробленого зараз — і відсіяти відновлені й догінні заходи з калібровки було
  неможливо в принципі.
- Таблиця ринку показує «ядра» в колонці «що тисне», коли темп ріже саме CPU:
  доти бокс із 20 шардами на 20 ядрах виглядав обмеженим шардами.

### Змінено

- **Реєстр боксів і калібрування шардів — дані користувача, а не пакета.**
  Адресу дає `GPURUNNER_REPO_DATA_DIR`, без неї — `<тека даних>/registry`. Доти
  реєстр жив у `data/` репозиторію й шукався відносно файла пакета — у
  встановленому колесі ця адреса вказувала в `site-packages`.
- **Нейтральні імена за замовчуванням:** бакет R2 `gpurunner-htr`, томи Modal
  `gpurunner-htr` і `gpurunner-spotter`. Свої задаються параметром
  `modal_volume`, змінними `GPURUNNER_MODAL_VOLUME[_<JOB>]`, `R2_BUCKET`.
- `htr_eval`: параметр `target` обов'язковий.
- `htr fetch-ckpt`: гілка декоду визначається правилом `out` / `out-<тег>`, а
  не зашитим переліком моделей.
- `requires-python >= 3.11` (було 3.12); версія пакета береться з одного місця.
- Бекенд Modal відмовляє ще до збирання образу, якщо локальний Python не збігається
  з версією образу (3.12): Modal серіалізує обгортку між інтерпретаторами.
- Довідка CLI (`--help`) — українською цілком.

### Виправлено

- `htr plan`: архів кадрів заливався під іменем теки, а посилання вело на ім'я
  прогону — перечитування справи іншою моделлю давало 404 у передпольоті.
- Після `--json` людський вивід консолі не «прилипає» до потоку попереднього
  виклику.
- `htr supervise --detach` на macOS: умова вимагала бінарника `setsid`, якого
  відчеплення не потребує (`start_new_session`). На POSIX вивід відчепленого
  процесу тепер іде в `spawn.log`, як на Windows, а не в `/dev/null`.
- Колесо збирається: `force-include` дублював файли пакета, і `uv build` падав.
- `mypy src` чистий (було 69 зауважень).
- **Кошторис занижувався саме на тих машинах, які ми купуємо тепер.** Модель
  темпу множила повний темп шарда на флот, який нема кому годувати: після того
  як 06.09 шард дістав право жити на одному ядрі, заходи пішли по 14–20 шардів
  на 19–23 ядрах — режиму, на якому калібрували, тоді не існувало. Спільна
  медіана прогноз/факт виглядала майже пристойно (1.06), і за нею ховався
  розрив **0.96 на старому раннері проти 1.27 на новому**. Лікує стеля за
  ЯДРАМИ: шард виходить на повний темп приблизно з двох ядер
  (`CORES_PER_SHARD_FULL_RATE = 1.8`), нижче — темп росте з ядрами, а не з
  флотом. Саме число підібране за смугою, де ядер бракує: без стелі вона дає
  1.27, при 1.5 — 1.11 (усе ще завищує), при 2.0 — 0.87 (карає дешеві
  малоядерні бокси — рівно та вада, через яку ворота ціни їх колись
  відкидали). ⚠ Не плутати з тим, скільки ядер шард ЗАЙМАЄ: там близько
  одного, і на 1.5 ядра шард працює — просто дає ~0.8 повного темпу.
  Замір: 113 заходів у смузі «ядер менше двох на шард» дають сталу величину
  рядко-годин на ЯДРО (~20 000), а не на шард; на фіксованому флоті 13–16
  шардів 15 ядер дали 261 536 рядко-год, 19 — 448 416, 27 — 562 297.
  🔴 Це НЕ повернення стелі, знятої 05.09: та була зростаюча й завищувала
  прогноз багатоядерним (525 проти 2000), ця — спадна й ніколи не піднімає
  прогноз вище за флотовий доданок.
  Разом із нею перекалібровано криву за матеріалом (`L0` 29 → 40,
  `A` 33 700 → 36 000). Результат: медіана 0.94 на 457 чистих заходах, обидві
  смуги («ядер менше двох на шард» і решта) сходяться на 0.94, вересневий зріз
  1.19 → 0.95, частка завищень понад 25% — 38% → 28%. Невідомий матеріал прогнозується як і доти (220 стор/год на
  шард), тож жоден старий приймач не зрушив.
- **Калібровка більше не рахує проби за заходи.** 59 записів із менш ніж 20
  прочитаними сторінками (проби, догони, два записи з нулем сторінок при
  додатному темпі) давали медіану 4.29 і тягли модель угору: вони міряють не
  темп, а фіксовану ціну справи, яку модель рахує окремо.

- **Вісім тестів розкладки були зелені на Windows і червоні на Linux.** Вони
  підставляли `os.cpu_count()`, а `_usable_cores()` бере ще й
  `len(os.sched_getaffinity(0))` — виклик, якого на Windows немає взагалі, а в
  CI він повертає справжні два ядра раннера й мовчки затирає підставлене
  число. Спільна фікстура `host_cores` ставить обидва джерела разом і додає
  `sched_getaffinity` там, де його в `os` нема, — тобто гілка прив'язки тепер
  перевіряється й на машині розробника. Сам раннер не міняється: він читав
  ядра правильно, брехали тести.

### Відоме про ім'я

- ⚠ **На PyPI пакет зветься `gpuhire`**, а команда, модуль і репозиторій —
  `gpurunner`. Ім'я `gpurunner` майданчик не дав узяти: воно надто схоже на
  наявний `gpu-runner` (0.0.7), і захист від typosquatting дивиться не на
  точний збіг. Перейменовувати команду заради цього не стали — вона стоїть у
  документації, у скілах Нишпорки й у змінній `NYSH_GPURUNNER`, а людина в
  терміналі набирає команду, не ім'я колеса.

### Прибрано

- Залежність `jinja2` і порожній пакет `templates` — не використовувались.

### Відомі обмеження

- Бібліотечний код друкує через `print`, окремого логера ще немає.
- Бекенди, що емулюють файлову систему Kaggle, дублюють частину коду.
- Перелік job'ів ведеться у двох місцях `jobs/__init__.py`.
- Загальної команди перевірки оточення (`doctor`) немає — лише
  `auth <бекенд> --verify` по одному.

